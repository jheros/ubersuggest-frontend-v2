// Components
import { Box } from '@mui/material';
// Styled
import { styled } from '@mui/system';

export const UberLogoStyled = styled(Box)({
  display: 'inline-flex',
  flexDirection: 'column',
  alignItems: 'flex-end',
});
